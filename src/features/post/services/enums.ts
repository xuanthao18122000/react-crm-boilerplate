export enum EnumPostType {
  DEFAULT = 1,
  ADVANCE,
}

export enum EnumPostCategoryType {
  INTRODUCTION = 2,
  LIBRARY = 5,
}

export enum EnumPostStatus {
  PENDING = -3,
  DRAFT = -2,
  CANCEL = -1,
  ACTIVE = 1,
}
